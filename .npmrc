# .npmrc
# Auto-approve specific build scripts
auto-approve-builds=sharp,unrs-resolver

# Allow scripts to run
enable-pre-post-scripts=true

# Ignore scripts during install (we'll run them separately)
ignore-scripts=true